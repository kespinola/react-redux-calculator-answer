import React, { PropTypes } from 'react'
import styles from './calculator.css';

const Calculator = props => {
  return (
      <div className={styles.root}>Boo World</div>
  );
};

export default Calculator;
